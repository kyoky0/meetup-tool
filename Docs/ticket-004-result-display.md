# Ticket-004: 結果表示・CSV ダウンロード

## 概要

解析結果を表形式で一覧表示し、CSV としてダウンロードできる機能。

## フェーズ

Phase 2

## ステータス

完了

---

## TODO

- [x] `st.dataframe` で結果を表形式表示（X）
- [x] カラム設定: 番号（small）/ 名前（medium）/ 会話要約（large）（X）
- [x] CSV を BOM 付き UTF-8 で生成（Excel での文字化け防止）（X）
- [x] `st.download_button` で CSV ダウンロードボタンを設置（X）
- [x] ファイル名: `meetup.csv`（X）
- [x] 名前をタップすると全文字起こしを展開表示（`st.expander` 使用）（X）
- [x] 結果を `meetup_session.json` に保存してページリロード後も復元（X）

---

## 仕様詳細

### 表示カラム

| カラム名 | 型     | 幅     |
| -------- | ------ | ------ |
| 番号     | Number | small  |
| 名前     | Text   | medium |
| 会話要約 | Text   | large  |

### CSV フォーマット

```
番号,名前,会話要約
1,山田太郎,〇〇株式会社のエンジニア。AIを使ったプロダクト開発をされている。
2,不明,スタートアップで営業をされている。近々資金調達を予定しているとのこと。
```

- エンコーディング: UTF-8 BOM（`utf-8-bom`）
- 区切り文字: カンマ

---

## 備考

- 解析完了後に `st.session_state.results` に保存
- ページを閉じると結果は消去される（永続化は未実装）
